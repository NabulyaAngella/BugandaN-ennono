# Frontend (Vite + React + Tailwind)

This folder contains a Vite React app preconfigured with Tailwind CSS.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The dev server runs on http://localhost:3000 by default.
