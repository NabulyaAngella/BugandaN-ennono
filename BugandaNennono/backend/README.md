# Backend (Express + Mongoose)

Quick start:

1. cd backend
2. npm install
3. copy `.env.example` to `.env` and adjust values
4. npm run dev

The backend listens on PORT from `.env` (default 4000). Example endpoint: `/api/example`.
