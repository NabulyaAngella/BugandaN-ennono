# Angella (monorepo)

This repository contains two top-level folders:

- `frontend` — Vite + React + Tailwind
- `backend` — Node.js (Express) + MongoDB (Mongoose)

See the READMEs in each folder for setup and run instructions.
